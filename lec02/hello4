#include <stdio.h>

int main()
{
	FILE *fout;
	fout = fopen("output.txt", "w");
	fprintf(fout, "Hello World\n"); // Comment
	fclose(fout);
	return 0;
}
#include <stdio.h>

int main()
{
	FILE *fout;
	fout = fopen("output.txt", "w");
	fprintf(fout, "Hello World\n"); // Comment
	fclose(fout);
	return 0;
}
#include <stdio.h>

int main()
{
	FILE *fout;
	fout = fopen("output.txt", "w");
	fprintf(fout, "Hello World\n"); // Comment
	fclose(fout);
	return 0;
}
